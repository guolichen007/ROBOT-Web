# 当前状态（唯一 SSOT）

> 本文件是仓库当前状态的**唯一人读真相源**。机器可读见 [approved-baseline.yaml](approved-baseline.yaml)。
> 历史快照/验收记录由 Git history + checkpoint tag 保留，不再散落在当前文档树。

## 封板锚点

```text
CODE_SEALED_SHA   = 584efcfda60b8d39c516a9939bc0481609fc6f3c
SERVER_RUNTIME_SHA= 584efcfda60b8d39c516a9939bc0481609fc6f3c
SERVER_DB         = 20260902_0008
SERVER_FINAL_SEAL = PASS
SAMPLE2           = OFFLINE
SAMPLE2_FIELD_ACCEPTANCE = PENDING
FINAL_RELEASE     = HOLD
```

- 不可变 checkpoint：annotated tag `baseline/server-runtime-2026-09-03` 指向 `584efcf…`。
- 服务器运行代码已封板；本仓库后续文档整理不得改变该 SHA 的运行语义。

## 当前架构

```text
Web → API → Dispatcher → MQTT → Vehicle Bridge → ROS → Control
```

- 软件控制核心链已证明（Server MQTT → Bridge → ROS → Control → feedback → ACK → Server）。
- 真实运动最终验收仍待样机现场（真实底盘 / odom / control_mode=3 / cmd_vel 仲裁 / 真实 STOP / 真实 PATROL）。
- 车端运行代码（Bridge / Control / firebotctl）与服务器部署脚本均为正式交付形态。

## 状态语义

- `CODE_SEALED_SHA`：当前封板的运行代码 SHA。
- `SERVER_FINAL_SEAL=PASS`：服务器运行代码已固化，非「现场验收完成」。
- `FINAL_RELEASE=HOLD`：未打正式 Release；需样机2 现场验收通过后再走 Release 流程。

## 待 OWNER 确认（HOLD，不阻塞只读接入）

以下现场参数平台不得猜测，须由 OWNER 运行态确认：

- vehicle ID、真实启动 launch / control_mode。
- 物理车头与 URDF `+X` 是否一致。
- map version / checksum。
- 主控管理 IP、物理急停。
- `/dev/ttyUSB1` 的 BMS / 烟雾归属。
- 视频与灭火硬件计划。

## 相关

- 唯一文档索引：见 [docs/README.md](../README.md)
