# 当前状态（唯一 SSOT）

> 本文件是仓库当前状态的**唯一人读真相源**。机器可读见 [approved-baseline.yaml](approved-baseline.yaml)。
> 历史快照/验收记录由 Git history + tag 保留，不再散落在当前文档树。

## 1. 版本锚点

```text
APPLICATION_CODE_BASELINE   = 4fd3e59d39190b6fab0688aa01e80f1e4cca4a86
BRANCH_CI_RUN               = Run76（34556036436，conclusion=success）
SERVER_RUNTIME_SHA          = 584efcfda60b8d39c516a9939bc0481609fc6f3c
SERVER_RUNTIME_TAG          = baseline/server-runtime-2026-09-03（→ 584efcf）
SERVER_DB                   = 20260902_0008
REPOSITORY_CONSOLIDATION_TAG = baseline/repository-consolidated-2026-09-11（→ 最终 main SHA，仓库治理封板后创建）
```

三个版本真相：

| 概念 | 值 | 含义 |
|---|---|---|
| `APPLICATION_CODE_BASELINE` | `4fd3e59…` | 最后通过软件 CI 的产品代码基线 |
| `FINAL_REPOSITORY_SHA` | 待定 | GitHub 最终仓库封板版本，可能比 4fd 新，但只增 docs/governance |
| `SERVER_RUNTIME_SHA` | `584efcf…` | 当前生产服务器真实运行版本 |

## 2. GitHub / 软件状态

- Run76 全绿：backend / frontend / protocol / ros1-vehicle / docs / containers / security / e2e（11/11）。
- Web renderer PASS（无 emitsOptions / runtime fatal）。
- STOP E2E PASS：STOP POST 202 → 任务取消 → STOP ACK → 5 条独立新鲜观测静止 → `VEHICLE_STATIONARY_CONFIRMED`。
- 多车隔离 PASS。
- 以上均为**软件层**验证，不构成真实底盘运动证明。

## 3. 服务器真实运行状态

- 服务器仍运行 `584efcf…`，DB 仍 `20260902_0008`。
- 仓库 `main` 即使更新，也不表示生产服务器升级；服务器只接受 exact SHA 部署。

## 4. 实车状态

```text
SAMPLE2                = OFFLINE
REAL_MOTION_VERIFIED   = NO
FIELD_ACCEPTANCE       = PENDING
```

真实底盘 / odom / `control_mode=3` / cmd_vel 仲裁 / 真实 STOP / 真实 PATROL 仍属于 field gate。

## 5. Release 状态

```text
FINAL_RELEASE = HOLD
```

Release 解锁条件：样机2 现场最终验收通过，并经 OWNER 授权。

## 6. 状态判断原则

```text
GitHub code truth    ≠  server runtime truth
server runtime truth ≠  vehicle field truth
vehicle field truth  ≠  formal product release truth
```

- 代码封板（code sealed）≠ 服务器已部署。
- 服务器已部署 ≠ 实车现场验收完成。
- 实车现场验收完成 ≠ 正式产品 Release。
