# 真实机器人接入检查清单（当前首车：ROS1 Noetic · Vehicle Bridge · Schema 1.3）

> 本清单是 **当前首车（ROS1 Noetic / Vehicle Bridge / Schema 1.3）** 的现场接入检查项，
> 只做 checklist，不复制完整 runbook。完整操作见 [车端Bridge部署与实车接口.md](车端Bridge部署与实车接口.md)；
> 当前状态真相源见 [实车现场联调总览.md](实车现场联调总览.md)；技术协议见 [FIREBOT_BRIDGE_CONTRACT_1.3.md](FIREBOT_BRIDGE_CONTRACT_1.3.md)。

---

## CURRENT VERIFIED STATE（2026-08-26，当前 Bridge-only 周期）

```text
Bridge communication = PASS
Bridge operation      = PASS
Broker reconnect      = PASS
Graceful stop         = PASS
LWT                   = PASS
Systemd recovery      = PASS
Short soak            = PASS
Long soak             = DEFERRED
Web UI                = NOT_CHECKED
ROS Provider          = NOT_STARTED
source_kind migration = NOT_STARTED
real control          = NOT_IMPLEMENTED
```

当前安全配置：

```text
BRIDGE_STUB_MODE=false
FIREBOT_SUPPORTED_COMMANDS=
FIREBOT_SENSORS=
FIREBOT_LOCATION_ENABLED=false
FIREBOT_FIELD_TRACE=false      ← 长期运行状态（R0–R4 临时 true 已结束）
```

当前运行形态：Bridge-only systemd，ROS isolated（`ROS_MASTER_URI=http://127.0.0.1:1`）。

```text
ROS_TOUCHED=NO
REAL_BATTERY_PROVIDER=NOT_VERIFIED
```

当前明确 NOT_EXECUTED 项：

```text
R2 late-roscore                       = NOT_EXECUTED_IN_CURRENT_BRIDGE_ONLY_CYCLE
R4 MANUAL_ROSTOPIC battery            = NOT_EXECUTED_IN_CURRENT_BRIDGE_ONLY_CYCLE
```

---

## 版本基线

- [ ] Bridge runtime SHA = `13c869247079b88da11b36666755906001a0041c`
- [ ] Server runtime SHA = `41bbaf4398711fd940dde1818193a67d34e355c8`

## 命令语义（重要）

- [ ] 当前 `supported_commands=[]`：任何命令在 validator 回 `COMMAND_UNSUPPORTED`，不会转发 ROS
- [ ] `BRIDGE_ADAPTER_NOT_CONNECTED` 仅在“命令已通过 capability 校验并成功转发 ROS 后无 feedback”时出现
- [ ] 只有 ROS 明确回 `ACCEPTED`，Bridge 才回 MQTT `accepted`

---

## LEGACY R0-R4 REVALIDATION APPENDIX（历史/复验，非当前下一步）

以下 R0–R4 项是**历史验收记录**，保留技术内容仅供将来复验；**当前 Bridge-only 周期不执行 ROS 相关项**。

- [ ] R1：无 roscore 时 MQTT 仍在线、heartbeat 持续、boot_id/PID 不变
- [ ] R2：Bridge 在线后启动 roscore，adapter 自动初始化，Bridge 不重启（当前周期 `NOT_EXECUTED`）
- [ ] R3：broker 受控断开/恢复（`OWNER_APPROVAL_REQUIRED`）
- [ ] R4：`MANUAL_ROSTOPIC` battery=67.5 链路到 Web（当前周期 `NOT_EXECUTED`；`REAL_BATTERY_PROVIDER=NOT_VERIFIED`）

---

ROS2 现场包是**另一套正式集成资产**（不是当前首台实车主路径），其接入见 [integration/ros2 现场对接说明](../integration/ros2/现场对接说明.md)。
