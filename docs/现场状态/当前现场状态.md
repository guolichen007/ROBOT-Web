# 当前现场状态（SSOT）

> 本文件是「当前现场状态」的唯一真相源。机器可读版本见 [approved-baseline.yaml](approved-baseline.yaml)。
> 旧快照（8/26、8/31）已迁移为历史证据，不再冒充当前状态。

## 版本与分支

- 开发分支：`integration/server-web-real-vehicle-ready-v1`
- 批准 SHA：以分支 HEAD 为准（`git rev-parse HEAD`），**不硬编码**；部署用 `FIREBOT_REQUIRE_SHA` 校验
- Bridge 安装目录：`/opt/firebot/vehicle-bridge`（唯一正式运行副本，旧 home 目录源码形态已废弃）
- Control 部署目录：`/home/tl/firerobot_ws/src/firebot_control`

## 控制能力（代码实现 ≠ 现场开放 ≠ 实车验收）

| 命令 | 代码实现 | 现场开放 | 实车验收 |
| --- | --- | --- | --- |
| PATROL_START | 已实现（software path PASS） | 否 | 否（REAL_MOTION_VERIFIED=NO） |
| STOP_MOTION | 已实现（fail-closed） | 否 | 否（CONTROL_FIELD_VERIFIED=NO） |
| emergency_stop | unavailable | 否 | 否 |
| reset_estop | unavailable | 否 | 否 |

## 遥测与安全链

- 真实 battery：NO（现场仍为 TEST_INJECTED）
- 真实 smoke：NO
- 真实速度链（`/odom → MQTT → 服务器`）：尚未现场验证（`motion_velocity_verified=false`）
- location：`FIREBOT_LOCATION_ENABLED=false`（地图身份未确认）
- cmd_vel 仲裁：尚未验证（`cmd_vel_arbitration_verified=false`）
- ROS_COMPAT downlink：`NOT_IMPLEMENTED`（仍只读）

## 已完成验收

- J5 服务器 online/freshness 现场验收：PASS
- Bridge 通信/运维/U2 freshness 现场验证：PASS（历史证据见验收记录文档）
- 车端运行路径收口（J6-R1/R1R1）：`/opt` 正式安装 + SHA 留痕

## 待办

- J6-A STOP 状态机测试 / J6-S1 task-worker 部署：PENDING（待服务器完整环境）
- 车端正式 60f/J6-R2 runtime：NOT_DEPLOYED（车端 Pull 最终 SHA → Bridge/Control 安装 → catkin_make → 静态 ROS 验证 → 再开放 stop_motion）

## 状态语义

- `code_implemented`：代码路径存在
- `config_enabled`：现场 `FIREBOT_SUPPORTED_COMMANDS` 等已开放
- `field_verified`：真实设备上已验收通过

三者必须分开表述，绝不把「代码实现」写成「现场已验收」。
